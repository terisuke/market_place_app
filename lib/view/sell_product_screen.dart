import 'package:flutter/material.dart';

class SellProductScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Sell a Product'),
      ),
      body: Center(
        child: Text('Sell Product Page'),
      ),
    );
  }
}
